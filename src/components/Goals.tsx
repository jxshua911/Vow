import { useEffect, useState, useCallback } from 'react';
import { supabase } from '@/lib/supabase';
import { useAuth } from '@/lib/auth';
import type { Goal, Milestone, Session, GoalStatus } from '@/types/database';
import { addDays, formatTime, formatDate, formatRelative } from '@/lib/dates';
import { allocateSameDaySlot, toOccupiedSlots } from '@/lib/scheduling';
import { PageHeader, NewButton } from './AppShell';
import { GoalPlanner } from './GoalPlanner';
import { GoalReferenceList } from './GoalReferenceList';
import { GoalResources } from '../../components/GoalResources';
type GlyphProps = { className?: string };
const makeGlyph = (symbol: string) => ({ className }: GlyphProps) => <span aria-hidden="true" className={`inline-flex items-center justify-center leading-none ${className || ''}`}>{symbol}</span>;
const Check = makeGlyph('✓'); const Circle = makeGlyph('○'); const CheckCircle2 = makeGlyph('✓'); const SkipForward = makeGlyph('›'); const Move = makeGlyph('↗'); const Pause = makeGlyph('Ⅱ'); const ChevronDown = makeGlyph('⌄'); const ArrowLeft = makeGlyph('←'); const Calendar = makeGlyph('□'); const Clock = makeGlyph('◷');
const OPEN_GOAL_PREFIX = 'vow:open-goal:';
type PlanDetails = { success_metric?: string; baseline?: string; progression?: string; summary?: string; assumptions?: string[]; checkpoints?: string[]; weekly_focus?: string[]; risks?: string[]; fallback_rules?: string[]; };
type ArmadilloDetails = { category?: string; goal_type?: string; metric?: string; target?: string; direction?: string; planning_strategy?: string; evidence?: string[]; };
function textList(value: unknown): string[] { return Array.isArray(value) ? value.filter((item): item is string => typeof item === 'string' && item.trim().length > 0) : []; }
export function GoalsPage() { const { session } = useAuth(); const [goals, setGoals] = useState<Goal[]>([]); const [selectedGoalId, setSelectedGoalId] = useState<string | null>(null); const [showCreate, setShowCreate] = useState(false); const [loading, setLoading] = useState(true); const [loadError, setLoadError] = useState(''); const loadGoals = useCallback(async () => { if (!session) return; setLoading(true); setLoadError(''); const { data, error } = await supabase.from('goals').select('*').eq('user_id', session.user.id).order('created_at', { ascending: false }); if (error) { setLoadError(error.message); setGoals([]); setLoading(false); return; } const nextGoals = data || []; setGoals(nextGoals); const requestedId = localStorage.getItem(`${OPEN_GOAL_PREFIX}${session.user.id}`); if (requestedId && nextGoals.some((goal) => goal.id === requestedId)) { setSelectedGoalId(requestedId); localStorage.removeItem(`${OPEN_GOAL_PREFIX}${session.user.id}`); } setLoading(false); }, [session]); useEffect(() => { loadGoals(); }, [loadGoals]); if (selectedGoalId) return <GoalDetail goalId={selectedGoalId} onBack={() => { setSelectedGoalId(null); loadGoals(); }} />; if (showCreate) return <GoalPlanner userId={session!.user.id} onCreated={() => { setShowCreate(false); loadGoals(); }} onCancel={() => setShowCreate(false)} />; const activeGoals = goals.filter((g) => g.status === 'active' || g.status === 'locked'); const draftGoals = goals.filter((g) => g.status === 'draft'); const completedGoals = goals.filter((g) => g.status === 'completed' || g.status === 'abandoned'); const goalCtaLabel = goals.length > 0 ? 'New Goal' : 'Create Goal'; return <div><PageHeader title="Goals" subtitle="Lock in commitments. Track honestly. Adjust when the plan is wrong." action={<NewButton onClick={() => setShowCreate(true)} label={goalCtaLabel} />} />{loadError && <p className="text-sm text-vow-ink border-l-2 border-vow-ink pl-3 mb-8 break-words" role="alert">Could not load your goals: {loadError}</p>}{loading ? <div className="text-vow-muted text-sm py-8">Loading your goals…</div> : goals.length === 0 ? <div className="border border-vow-border p-10 md:p-16 text-center"><p className="vow-heading text-xl text-vow-ink mb-2">No goals yet</p><p className="text-vow-muted text-sm leading-relaxed max-w-md mx-auto">Create your first commitment and let VOW map the work into your calendar.</p></div> : <div className="space-y-12">{activeGoals.length > 0 && <GoalSection title="Active" goals={activeGoals} onSelect={setSelectedGoalId} />}{draftGoals.length > 0 && <GoalSection title="Drafts" goals={draftGoals} onSelect={setSelectedGoalId} />}{completedGoals.length > 0 && <GoalSection title="Completed & Abandoned" goals={completedGoals} onSelect={setSelectedGoalId} />}</div>}</div>; }
function GoalSection({ title, goals, onSelect }: { title: string; goals: Goal[]; onSelect: (id: string) => void }) { const statusColors: Record<GoalStatus, string> = { draft: 'text-vow-muted', locked: 'text-vow-ink', active: 'text-vow-ink', completed: 'text-vow-success', abandoned: 'text-vow-muted line-through' }; return <div><h2 className="vow-label mb-4">{title}</h2><div className="border-t border-vow-border">{goals.map((g) => <button key={g.id} onClick={() => onSelect(g.id)} className="w-full min-h-20 text-left border-b border-vow-border py-5 group flex items-start justify-between gap-4 hover:bg-vow-surface/30 transition-colors"><div className="flex-1 min-w-0"><div className={`text-base font-medium mb-1 ${statusColors[g.status]} break-words`}>{g.outcome}</div>{g.why_it_matters && <div className="text-xs text-vow-muted italic mb-2 break-words">"{g.why_it_matters}"</div>}<div className="flex items-center gap-4 text-xs text-vow-muted flex-wrap"><span className="flex items-center gap-1"><Calendar className="w-3 h-3" />{g.deadline ? formatRelative(g.deadline) : 'No deadline'}</span><span className="flex items-center gap-1"><Clock className="w-3 h-3" />{g.weekly_commitment_target}x/week</span></div></div><ArrowLeft className="w-11 h-11 p-3 text-vow-border rotate-180 shrink-0 group-hover:text-vow-ink transition-colors" /></button>)}</div></div>; }
function GoalDetail({ goalId, onBack }: { goalId: string; onBack: () => void }) { const { session } = useAuth(); const [goal, setGoal] = useState<Goal | null>(null); const [milestones, setMilestones] = useState<Milestone[]>([]); const [sessions, setSessions] = useState<Session[]>([]); const [loading, setLoading] = useState(true); const [showPauseModal, setShowPauseModal] = useState(false); const [expandedMilestone, setExpandedMilestone] = useState<string | null>(null); const [editingSessionId, setEditingSessionId] = useState<string | null>(null); const [editingTime, setEditingTime] = useState(''); const [sessionEditError, setSessionEditError] = useState(''); const load = useCallback(async () => { if (!session) return; const [goalRes, msRes, sessRes] = await Promise.all([supabase.from('goals').select('*').eq('id', goalId).eq('user_id', session.user.id).maybeSingle(), supabase.from('milestones').select('*').eq('goal_id', goalId).eq('user_id', session.user.id).order('sort_order'), supabase.from('sessions').select('*').eq('goal_id', goalId).eq('user_id', session.user.id).order('scheduled_at', { ascending: true })]); if (goalRes.error || msRes.error || sessRes.error) { setLoading(false); return; } setGoal(goalRes.data as Goal | null); setMilestones(msRes.data || []); setSessions(sessRes.data || []); setLoading(false); }, [goalId, session]); useEffect(() => { load(); }, [load]); async function updateSessionStatus(sessId: string, status: Session['status']) { if (!session) return; const updates: Partial<Session> = { status, updated_at: new Date().toISOString() }; if (status === 'completed') updates.completed_at = new Date().toISOString(); const { error } = await supabase.from('sessions').update(updates).eq('id', sessId).eq('user_id', session.user.id); if (!error) await load(); } async function moveSession(sess: Session) { if (!session) return; const newDate = addDays(new Date(sess.scheduled_at), 1); const { error } = await supabase.from('sessions').update({ scheduled_at: newDate.toISOString(), moved_count: sess.moved_count + 1, status: 'moved', updated_at: new Date().toISOString() }).eq('id', sess.id).eq('user_id', session.user.id); if (!error) await load(); } function beginEditTime(sess: Session) { const date = new Date(sess.scheduled_at); setEditingSessionId(sess.id); setEditingTime(`${String(date.getHours()).padStart(2, '0')}:${String(date.getMinutes()).padStart(2, '0')}`); setSessionEditError(''); } async function saveSessionTime(sess: Session) { if (!session || !/^\d{2}:\d{2}$/.test(editingTime)) return; const [hours, minutes] = editingTime.split(':').map(Number); const nextDate = new Date(sess.scheduled_at); nextDate.setHours(hours, minutes, 0, 0); const { data: conflicts, error: conflictError } = await supabase.from('sessions').select('scheduled_at,duration_minutes').eq('user_id', session.user.id).eq('status', 'scheduled').neq('id', sess.id); if (conflictError) { setSessionEditError(conflictError.message); return; } const occupied = toOccupiedSlots((conflicts || []).map((row) => ({ scheduledAt: row.scheduled_at, durationMinutes: row.duration_minutes }))); const exact = allocateSameDaySlot(nextDate, sess.duration_minutes, occupied); if (!exact || exact.date.getTime() !== nextDate.getTime()) { setSessionEditError('That time overlaps another commitment. Choose another time.'); return; } const { error } = await supabase.from('sessions').update({ scheduled_at: nextDate.toISOString(), updated_at: new Date().toISOString() }).eq('id', sess.id).eq('user_id', session.user.id); if (error) { setSessionEditError(error.message); return; } await supabase.from('goal_plan_items').update({ scheduled_at: nextDate.toISOString(), updated_at: new Date().toISOString() }).eq('goal_id', sess.goal_id).eq('user_id', session.user.id).eq('scheduled_at', sess.scheduled_at); setEditingSessionId(null); setEditingTime(''); setSessionEditError(''); await load(); } async function toggleMilestoneStatus(ms: Milestone) { if (!session) return; const newStatus = ms.status === 'completed' ? 'pending' : 'completed'; const { error } = await supabase.from('milestones').update({ status: newStatus, updated_at: new Date().toISOString() }).eq('id', ms.id).eq('user_id', session.user.id); if (!error) await load(); } async function abandonGoal() { if (!session || !confirm('Mark this goal as abandoned? This records the outcome so VOW can learn from the pattern.')) return; const { error } = await supabase.from('goals').update({ status: 'abandoned', updated_at: new Date().toISOString() }).eq('id', goalId).eq('user_id', session.user.id); if (!error) onBack(); } async function completeGoal() { if (!session) return; const { error } = await supabase.from('goals').update({ status: 'completed', updated_at: new Date().toISOString() }).eq('id', goalId).eq('user_id', session.user.id); if (!error) onBack(); } async function handlePause(context: string) { if (!session) return; const { error } = await supabase.from('user_settings').upsert({ user_id: session.user.id, pause_context: context || null, updated_at: new Date().toISOString() }, { onConflict: 'user_id' }); if (!error) setShowPauseModal(false); } if (loading) return <div className="text-vow-muted text-sm py-8">Loading goal…</div>; if (!goal) return <div className="text-vow-muted text-sm py-8">We couldn't find this goal.</div>; const completedSessions = sessions.filter((s) => s.status === 'completed').length; const totalSessions = sessions.length; const pct = totalSessions > 0 ? Math.round((completedSessions / totalSessions) * 100) : 0; const movedCount = sessions.filter((s) => s.moved_count > 0).reduce((sum, s) => sum + s.moved_count, 0); const statusIcons: Record<string, typeof CheckCircle2> = { completed: CheckCircle2, scheduled: Circle, skipped: SkipForward, moved: Move }; const plan = (goal.plan_json || {}) as PlanDetails; const armadillo = (goal.armadillo || {}) as ArmadilloDetails; const weeklyFocus = textList(plan.weekly_focus); const checkpoints = textList(plan.checkpoints); const assumptions = textList(plan.assumptions); const risks = textList(plan.risks); const fallbackRules = textList(plan.fallback_rules); const evidence = textList(armadillo.evidence);
  return <div className="min-w-0 overflow-hidden"><button onClick={onBack} className="min-h-11 text-sm text-vow-muted hover:text-vow-ink mb-6 flex items-center gap-1 transition-colors"><ArrowLeft className="w-5 h-5" />Back to goals</button><div className="border-t border-vow-border pt-8 mb-8"><div className="flex items-start justify-between gap-4 mb-4"><h1 className="vow-heading text-2xl text-vow-ink min-w-0 break-words">{goal.outcome}</h1><span className="text-xs uppercase tracking-wide text-vow-muted shrink-0">{goal.status}</span></div>{goal.why_it_matters && <p className="text-sm text-vow-muted italic mb-6 break-words">"{goal.why_it_matters}"</p>}<div className="grid grid-cols-3 gap-4 border-t border-vow-border pt-4"><div><p className="vow-label">Progress</p><p className="text-lg text-vow-ink mt-1">{pct}%</p></div><div><p className="vow-label">Sessions</p><p className="text-lg text-vow-ink mt-1">{completedSessions}/{totalSessions}</p></div><div><p className="vow-label">Moved</p><p className="text-lg text-vow-ink mt-1">{movedCount}</p></div></div></div><div className="mb-10 min-w-0"><div className="border-t border-vow-border pt-8"><p className="vow-label mb-4">VOW plan & progression</p>{plan.summary && <p className="text-sm text-vow-ink leading-relaxed break-words mb-5">{plan.summary}</p>}<div className="grid gap-4 md:grid-cols-2">{plan.success_metric && <div className="border border-vow-border p-4 min-w-0"><p className="vow-label mb-2">Success metric</p><p className="text-sm text-vow-ink break-words">{plan.success_metric}</p></div>}{plan.baseline && <div className="border border-vow-border p-4 min-w-0"><p className="vow-label mb-2">Baseline</p><p className="text-sm text-vow-ink break-words">{plan.baseline}</p></div>}</div>{plan.progression && <div className="border border-vow-border p-4 mt-4 min-w-0"><p className="vow-label mb-2">Progression notes</p><p className="text-sm text-vow-ink leading-relaxed break-words">{plan.progression}</p></div>}{weeklyFocus.length > 0 && <div className="mt-6"><p className="vow-label mb-3">Weekly focus</p><div className="space-y-2">{weeklyFocus.map((item, index) => <div key={index} className="border-b border-vow-border py-3 text-sm text-vow-ink break-words">{item}</div>)}</div></div>}{checkpoints.length > 0 && <div className="mt-6"><p className="vow-label mb-3">Checkpoints</p><ul className="space-y-2">{checkpoints.map((item, index) => <li key={index} className="text-sm text-vow-muted break-words">{item}</li>)}</ul></div>}{(assumptions.length > 0 || risks.length > 0 || fallbackRules.length > 0) && <div className="grid gap-4 md:grid-cols-3 mt-6">{assumptions.length > 0 && <div className="border border-vow-border p-4 min-w-0"><p className="vow-label mb-2">Assumptions</p><ul className="space-y-2">{assumptions.map((item, index) => <li key={index} className="text-xs text-vow-muted break-words">{item}</li>)}</ul></div>}{risks.length > 0 && <div className="border border-vow-border p-4 min-w-0"><p className="vow-label mb-2">Watch-outs</p><ul className="space-y-2">{risks.map((item, index) => <li key={index} className="text-xs text-vow-muted break-words">{item}</li>)}</ul></div>}{fallbackRules.length > 0 && <div className="border border-vow-border p-4 min-w-0"><p className="vow-label mb-2">If a session is missed</p><ul className="space-y-2">{fallbackRules.map((item, index) => <li key={index} className="text-xs text-vow-muted break-words">{item}</li>)}</ul></div>}</div>}</div></div><div className="mb-10 min-w-0"><div className="border-t border-vow-border pt-8"><p className="vow-label mb-4">Goal interpretation</p><div className="grid grid-cols-2 gap-px bg-vow-border border border-vow-border md:grid-cols-3">{([['Category', armadillo.category], ['Goal type', armadillo.goal_type], ['Metric', armadillo.metric], ['Target', armadillo.target], ['Direction', armadillo.direction], ['Planning strategy', armadillo.planning_strategy]] as Array<[string, string | undefined]>).filter(([, value]) => value).map(([label, value]) => <div key={label} className="bg-vow-bg p-4 min-w-0"><p className="vow-label mb-1">{label}</p><p className="text-sm text-vow-ink break-words">{value}</p></div>)}</div>{evidence.length > 0 && <div className="mt-4 border border-vow-border p-4 min-w-0"><p className="vow-label mb-2">Evidence used</p><ul className="space-y-2">{evidence.map((item, index) => <li key={index} className="text-xs text-vow-muted break-words">{item}</li>)}</ul></div>}</div></div><GoalResources goalId={goalId} /><GoalReferenceList goalId={goalId} /><div className="mb-10"><div className="flex items-center justify-between mb-4"><h2 className="vow-label">Milestones</h2><button onClick={() => setExpandedMilestone(expandedMilestone ? null : milestones[0]?.id || null)} className="vow-btn-soft min-h-11 text-xs">{expandedMilestone ? 'Collapse' : 'Expand'}</button></div><div className="border-t border-vow-border">{milestones.map((ms) => { const Icon = ms.status === 'completed' ? CheckCircle2 : Circle; return <div key={ms.id} className="border-b border-vow-border py-4"><button className="w-full min-h-11 text-left flex items-start gap-3 min-w-0" onClick={() => setExpandedMilestone(expandedMilestone === ms.id ? null : ms.id)}><Icon className="w-5 h-5 mt-0.5 shrink-0" /><div className="flex-1 min-w-0"><p className="text-sm text-vow-ink break-words">{ms.title}</p><p className="text-xs text-vow-muted mt-1">{formatDate(ms.deadline)}</p>{expandedMilestone === ms.id && <p className="text-xs text-vow-muted mt-2 break-words leading-relaxed">{ms.description}</p>}</div><ChevronDown className={`w-11 h-11 p-3 transition-transform shrink-0 ${expandedMilestone === ms.id ? 'rotate-180' : ''}`} /></button><button onClick={() => toggleMilestoneStatus(ms)} className="vow-btn-soft min-h-11 text-xs ml-8 mt-2">{ms.status === 'completed' ? 'Mark pending' : 'Mark completed'}</button></div>; })}</div></div><div className="mb-10"><div className="flex items-center justify-between mb-4"><h2 className="vow-label">Sessions & routine</h2></div><div className="border-t border-vow-border">{sessions.length === 0 ? <div className="py-6 text-sm text-vow-muted">No sessions scheduled for this goal yet.</div> : sessions.map((sess) => { const Icon = statusIcons[sess.status] || Circle; const editing = editingSessionId === sess.id; return <div key={sess.id} className="border-b border-vow-border py-4 min-w-0"><div className="flex items-start gap-3 min-w-0"><Icon className="w-5 h-5 text-vow-muted shrink-0 mt-1" /><div className="flex-1 min-w-0 overflow-hidden"><p className="text-sm text-vow-ink break-words">{sess.title}</p>{!editing && <p className="text-xs text-vow-muted mt-1 break-words">{formatDate(sess.scheduled_at)} · {formatTime(sess.scheduled_at)} · {sess.duration_minutes} min</p>}{editing && <div className="mt-3 flex items-center gap-2 flex-wrap"><input type="time" value={editingTime} onChange={(e) => setEditingTime(e.target.value)} aria-label="New session time" className="vow-input w-auto min-w-[140px]" autoFocus /><button onClick={() => saveSessionTime(sess)} disabled={!/^\d{2}:\d{2}$/.test(editingTime)} className="vow-btn-primary text-xs">Save time</button><button onClick={() => { setEditingSessionId(null); setEditingTime(''); setSessionEditError(''); }} className="vow-btn-ghost text-xs">Cancel</button></div>}</div>{sess.status === 'scheduled' && !editing && <div className="flex flex-wrap gap-2 justify-end shrink-0"><button onClick={() => beginEditTime(sess)} className="vow-btn-soft min-h-11 text-xs">Change time</button><button onClick={() => updateSessionStatus(sess.id, 'completed')} className="vow-btn-soft min-h-11 text-xs">Complete</button><button onClick={() => updateSessionStatus(sess.id, 'skipped')} className="vow-btn-soft min-h-11 text-xs">Skip</button><button onClick={() => moveSession(sess)} className="vow-btn-soft min-h-11 text-xs">Move</button></div>}</div>{editing && sessionEditError && <p className="text-xs text-vow-ink border-l-2 border-vow-ink pl-3 mt-3 ml-8 break-words" role="alert">{sessionEditError}</p>}</div>; })}</div></div><div className="border-t border-vow-border pt-6 mt-10 flex flex-wrap gap-3"><button onClick={completeGoal} className="vow-btn-primary"><Check className="w-4 h-4" />Complete goal</button><button onClick={() => setShowPauseModal(true)} className="vow-btn-ghost"><Pause className="w-4 h-4" />Pause</button><button onClick={abandonGoal} className="vow-btn-ghost">Abandon</button></div>{showPauseModal && <div className="fixed inset-0 bg-black/20 flex items-center justify-center p-4 z-50" role="dialog" aria-modal="true"><div className="bg-vow-bg border border-vow-border p-6 max-w-md w-full"><h2 className="vow-heading text-lg text-vow-ink mb-2">Pause goal</h2><p className="text-sm text-vow-muted mb-4">Why are you pausing?</p><div className="space-y-2">{['Schedule changed','Need to adjust the plan','Taking a short break'].map((reason) => <button key={reason} onClick={() => handlePause(reason)} className="w-full min-h-11 text-left border border-vow-border px-4 py-3 text-sm hover:bg-vow-surface">{reason}</button>)}</div><button onClick={() => setShowPauseModal(false)} className="vow-btn-ghost min-h-11 mt-4">Cancel</button></div></div>}</div>; }
