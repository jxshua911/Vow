import { useState } from 'react';
import { FunctionsHttpError } from '@supabase/supabase-js';
import { supabase } from '@/lib/supabase';
import { addDays, toDateString } from '@/lib/dates';
import { syncUpcomingSessionNotifications } from '@/lib/notifications';
import type { Session } from '@/types/database';
import { analyseGoalForEvidence, type ArmadilloResult } from '@/lib/armadillo';
import { PageHeader } from './AppShell';

type Clarification = { questions: string[]; recommended_duration_weeks: number; rationale: string };
type PlanItem = { week: number; day: string; task: string; purpose: string; target_metric: string; duration_minutes: number; preferred_time: string };
type Plan = { outcome: string; success_metric: string; baseline: string; assumptions: string[]; milestones: Array<{ title: string; description: string; week: number }>; schedule: PlanItem[]; progression: string; checkpoints: string[]; risks: string[]; fallback_rules: string[]; summary: string; duration_weeks: number; weekly_commitment_target: number; available_days: string[]; generation_mode?: 'groq'|'fallback'; generation_error?: string|null; armadillo?: ArmadilloResult };
const DAYS = ['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];
const DURATIONS = [{ weeks: 1, label: '1 week', detail: 'Quick start' }, { weeks: 2, label: '2 weeks', detail: 'Short sprint' }, { weeks: 4, label: '1 month', detail: 'Build momentum' }, { weeks: 8, label: '2 months', detail: 'Build consistency' }, { weeks: 12, label: '3 months', detail: 'Meaningful change' }, { weeks: 26, label: '6 months', detail: 'Long-term build' }, { weeks: 52, label: '1 year', detail: 'Full-year commitment' }];
const ARMADILLO_DEBUG_USER_ID = 'ed46f41a-0584-4758-843f-2f9f337e5598';
function nextMonday() { const d = new Date(); const day = d.getDay(); d.setHours(0,0,0,0); d.setDate(d.getDate() + (day === 0 ? 1 : 8 - day)); return d; }
function deadlineFor(start: Date, weeks: number) { return toDateString(addDays(start, weeks * 7 - 1)); }
function durationLabel(weeks: number) { return DURATIONS.find(x => x.weeks === weeks)?.label || `${weeks} weeks`; }
async function functionError(error: unknown) { if (error instanceof FunctionsHttpError) { try { const body = await error.context.json(); return body?.error || body?.message || JSON.stringify(body); } catch { return error.message; } } return error instanceof Error ? error.message : String(error || ''); }

export function GoalPlanner({ userId, onCreated, onCancel }: { userId: string; onCreated: () => void; onCancel?: () => void }) {
  const [rawInput,setRawInput]=useState(''); const [why,setWhy]=useState(''); const [durationWeeks,setDurationWeeks]=useState(8); const [availableDays,setAvailableDays]=useState(['Monday','Wednesday','Saturday']);
  const [clarification,setClarification]=useState<Clarification|null>(null); const [answers,setAnswers]=useState<string[]>([]); const [plan,setPlan]=useState<Plan|null>(null); const [draftGoalId,setDraftGoalId]=useState<string|null>(null); const [armadillo,setArmadillo]=useState<ArmadilloResult|null>(null); const [busy,setBusy]=useState(false); const [saving,setSaving]=useState(false); const [error,setError]=useState('');
  function toggleDay(day:string){setAvailableDays(x=>x.includes(day)?x.filter(d=>d!==day):x.length<7?[...x,day]:x);}
  async function ensureDraft(){if(draftGoalId)return draftGoalId;const start=nextMonday();const {data,error:e}=await supabase.from('goals').insert({user_id:userId,title:rawInput.trim(),outcome:rawInput.trim(),why_it_matters:why.trim()||null,start_date:toDateString(start),deadline:deadlineFor(start,durationWeeks),duration:`${durationWeeks}w`,status:'draft',weekly_commitment_target:availableDays.length}).select('id').single();if(e||!data)throw e||new Error('Could not start this goal.');setDraftGoalId(data.id);return data.id;}
  async function getArmadillo(){const fallback=analyseGoalForEvidence({title:rawInput.trim(),outcome:rawInput.trim(),why_it_matters:why.trim()||null});const {data,error:e}=await supabase.rpc('armadillo_analyse_goal',{goal_title:rawInput.trim(),goal_outcome:rawInput.trim(),goal_why:why.trim()||null});const result=!e&&data?data as ArmadilloResult:fallback;setArmadillo(result);return result;}
  async function loadReferences(goalId:string){const {data}=await supabase.from('goal_resources').select('url,title,resource_type').eq('goal_id',goalId).order('created_at',{ascending:false}).limit(4);return data||[];}
  async function requestPlan(a:ArmadilloResult,goalId:string,clarificationAnswers:Array<{question:string;answer:string}>){const references=await loadReferences(goalId);const {data,error:e}=await supabase.functions.invoke('vow-goal-ai',{body:{mode:'goal-plan',goal:{id:goalId,title:rawInput.trim(),outcome:rawInput.trim(),why_it_matters:why.trim()||null,start_date:toDateString(nextMonday()),deadline:deadlineFor(nextMonday(),durationWeeks),duration_weeks:durationWeeks,weekly_commitment_target:availableDays.length},armadillo:a,answers:clarificationAnswers,available_days:availableDays,references,message:`Build the final VOW plan using Armadillo as the canonical goal interpretation. Follow-up answers: ${JSON.stringify(clarificationAnswers)}. Duration: ${durationLabel(durationWeeks)}. Available days: ${availableDays.join(', ')}. Do not reinterpret the goal outside Armadillo's category, goal type, target, metric, direction, evidence and planning strategy.`}});if(e||!data?.structured)throw new Error(await functionError(e)||data?.error||'VOW AI could not build the plan.');const next=data.structured as Plan;if(next.duration_weeks!==durationWeeks||!Array.isArray(next.schedule)||!next.schedule.length)throw new Error('VOW AI returned an incomplete plan.');next.armadillo=data.armadillo||a;setPlan(next);}
  async function startPlanning(){if(!rawInput.trim()||busy||availableDays.length===0)return;setBusy(true);setError('');try{const goalId=await ensureDraft();const a=await getArmadillo();if(!a.needs_clarification){await requestPlan(a,goalId,[]);return;}const references=await loadReferences(goalId);const {data,error:e}=await supabase.functions.invoke('vow-goal-ai',{body:{mode:'goal-clarify',goal:{id:goalId,title:rawInput.trim(),outcome:rawInput.trim(),why_it_matters:why.trim()||null,duration_weeks:durationWeeks,weekly_commitment_target:availableDays.length},armadillo:a,available_days:availableDays,references,message:'Generate at most 3 goal-specific clarification questions from this Armadillo interpretation. Ask only for information that materially improves the plan. Do not ask for the goal, duration, selected days, category, goal type or metric again.'}});if(e||!data?.structured||!Array.isArray(data.structured.questions)||!data.structured.questions.length)throw new Error(await functionError(e)||data?.error||'VOW AI could not prepare the follow-up questions.');const next=data.structured as Clarification;next.questions=next.questions.slice(0,3);setClarification(next);setAnswers(next.questions.map(()=>''));await supabase.from('goal_clarification_answers').delete().eq('goal_id',goalId);const {error:ie}=await supabase.from('goal_clarification_answers').insert(next.questions.map((question,index)=>({goal_id:goalId,user_id:userId,question,answer:null,question_order:index})));if(ie)throw ie;}catch(err){setError(err instanceof Error?err.message:'Could not start this goal.');}finally{setBusy(false);}}
  async function buildPlan(){if(!clarification||busy)return;setBusy(true);setError('');try{const goalId=await ensureDraft();const clean=answers.map(a=>a.trim());await supabase.from('goal_clarification_answers').delete().eq('goal_id',goalId);const {error:ie}=await supabase.from('goal_clarification_answers').insert(clarification.questions.map((question,index)=>({goal_id:goalId,user_id:userId,question,answer:clean[index]||null,question_order:index})));if(ie)throw ie;await requestPlan(armadillo||await getArmadillo(),goalId,clarification.questions.map((question,index)=>({question,answer:clean[index]||''})));}catch(err){setError(err instanceof Error?err.message:'VOW AI could not build the plan.');}finally{setBusy(false);}}
  async function handleCreate(){if(!plan||!draftGoalId||saving)return;setSaving(true);setError('');try{const start=nextMonday();const version=1;const {error:ge}=await supabase.from('goals').update({title:rawInput.trim(),outcome:plan.outcome||rawInput.trim(),why_it_matters:why.trim()||null,start_date:toDateString(start),deadline:deadlineFor(start,durationWeeks),duration:`${durationWeeks}w`,status:'active',weekly_commitment_target:availableDays.length,armadillo:plan.armadillo||armadillo,armadillo_analyzed_at:new Date().toISOString(),plan_json:plan,plan_version:version,plan_generated_at:new Date().toISOString(),planning_horizon_weeks:durationWeeks,planning_timezone:Intl.DateTimeFormat().resolvedOptions().timeZone}).eq('id',draftGoalId);if(ge)throw ge;await supabase.from('goal_plan_items').delete().eq('goal_id',draftGoalId);await supabase.from('sessions').delete().eq('goal_id',draftGoalId);await supabase.from('milestones').delete().eq('goal_id',draftGoalId);const {data:milestones,error:me}=await supabase.from('milestones').insert(plan.milestones.map((m,i)=>({goal_id:draftGoalId,title:m.title,description:m.description,sort_order:i,deadline:deadlineFor(start,Math.min(durationWeeks,Math.max(1,m.week))),status:i===0?'in_progress':'pending'}))).select('id,sort_order').order('sort_order');if(me)throw me;const milestoneFor=(week:number)=>{const list=milestones||[];if(!list.length)return null;const index=Math.min(list.length-1,Math.floor(((Math.max(1,week)-1)/Math.max(1,durationWeeks))*list.length));return list[index]?.id||null;};const schedule=plan.schedule.filter(item=>availableDays.includes(item.day)).map(item=>{const week=Math.max(1,Math.min(durationWeeks,Number(item.week)||1));const dayIndex=Math.max(0,DAYS.indexOf(item.day));const date=addDays(start,(week-1)*7+dayIndex);const match=/^(\d{1,2}):(\d{2})/.exec(item.preferred_time||'09:00');date.setHours(Math.min(23,Number(match?.[1]||9)),Math.min(59,Number(match?.[2]||0)),0,0);return{item,week,date};});if(!schedule.length)throw new Error('The generated schedule does not match your selected days.');const {data:planRows,error:pe}=await supabase.from('goal_plan_items').insert(schedule.map(({item,week,date})=>({goal_id:draftGoalId,user_id:userId,plan_version:version,week_number:week,day_of_week:item.day,scheduled_at:date.toISOString(),task:item.task,purpose:item.purpose||null,target_metric:item.target_metric||plan.success_metric,duration_minutes:Math.max(5,Number(item.duration_minutes)||30),status:'scheduled'}))).select('*');if(pe)throw pe;const sessionRows=schedule.map(({item,week,date},index)=>({goal_id:draftGoalId,milestone_id:milestoneFor(week),user_id:userId,title:item.task,scheduled_at:date.toISOString(),duration_minutes:Math.max(5,Number(item.duration_minutes)||30),status:'scheduled',moved_count:0,notes:[item.purpose,item.target_metric?`Target: ${item.target_metric}`:null].filter(Boolean).join('\n')||null,external_event_id:planRows?.[index]?.external_event_id||null}));const {data:sessions,error:se}=await supabase.from('sessions').insert(sessionRows).select('*');if(se)throw se;try{if(sessions)await syncUpcomingSessionNotifications(sessions as Session[]);}catch{console.warn('[VOW] Session notifications could not be synced.');}const {data:calendar,error:ce}=await supabase.functions.invoke('google-calendar-sync-goal',{body:{goalId:draftGoalId}});if(ce)console.warn('[VOW] Google Calendar sync unavailable or not connected.',ce);else if(calendar?.created_count)console.info(`[VOW] ${calendar.created_count} Google Calendar events created.`);onCreated();}catch(err){setError(err instanceof Error?err.message:'Failed to create goal.');}finally{setSaving(false);}}
  if(plan)return <div><button onClick={()=>setPlan(null)} className="text-sm text-vow-muted hover:text-vow-ink mb-6">← Adjust answers</button><PageHeader title="Your VOW plan" subtitle={`${durationLabel(durationWeeks)} · ${availableDays.length} sessions/week`} /><div className="max-w-3xl space-y-6"><section className="border border-vow-border p-5"><p className="vow-label mb-2">Outcome</p><p className="text-lg font-medium text-vow-ink">{plan.outcome}</p><p className="text-sm text-vow-muted mt-3">{plan.summary}</p></section>{userId===ARMADILLO_DEBUG_USER_ID&&plan.armadillo&&<section className="border border-vow-border p-5"><p className="vow-label mb-3">Armadillo intelligence</p><div className="grid grid-cols-2 sm:grid-cols-4 gap-3 text-sm">{[['Category',plan.armadillo.category],['Goal type',plan.armadillo.goal_type],['Metric',plan.armadillo.metric],['Target',plan.armadillo.target||'—']].map(([label,value])=><div key={label}><p className="text-xs text-vow-muted">{label}</p><p className="text-vow-ink mt-1">{value}</p></div>)}</div><p className="text-xs text-vow-muted mt-4">Evidence: {plan.armadillo.evidence.join(', ')}{plan.armadillo.integration?` · Suggested integration: ${plan.armadillo.integration}`:''}</p>{plan.armadillo.time_target&&<p className="text-xs text-vow-muted mt-1">Time target: {plan.armadillo.time_target}</p>}<p className="text-xs text-vow-muted mt-1">Generation: {plan.generation_mode||'unknown'}</p>{plan.generation_error&&<p className="text-xs text-vow-muted mt-1">Generation error: {plan.generation_error}</p>}</section>}<section className="border border-vow-border p-5"><p className="vow-label mb-2">Success metric</p><p className="text-sm text-vow-ink">{plan.success_metric}</p><p className="vow-label mt-5 mb-2">Baseline</p><p className="text-sm text-vow-muted">{plan.baseline}</p></section><section className="border border-vow-border p-5"><p className="vow-label mb-4">Schedule · {durationLabel(durationWeeks)}</p><div className="divide-y divide-vow-border">{plan.schedule.map((item,i)=><div key={`${item.week}-${item.day}-${i}`} className="py-3 flex gap-3"><span className="text-xs text-vow-muted w-12 shrink-0">W{item.week}</span><span className="text-xs text-vow-muted w-20 shrink-0">{item.day}</span><div><p className="text-sm text-vow-ink font-medium">{item.task}</p><p className="text-xs text-vow-muted mt-1">{item.duration_minutes} min · {item.target_metric}</p></div></div>)}</div></section><section className="border border-vow-border p-5"><p className="vow-label mb-4">Milestones & progression</p>{plan.milestones.map(m=><div key={`${m.week}-${m.title}`} className="mb-4"><p className="text-sm text-vow-ink font-medium">Week {m.week} · {m.title}</p><p className="text-xs text-vow-muted mt-1">{m.description}</p></div>)}<p className="text-sm text-vow-muted pt-4 border-t border-vow-border">{plan.progression}</p></section>{error&&<p className="text-sm text-vow-ink border-l-2 border-vow-ink pl-3">{error}</p>}<div className="flex gap-3"><button onClick={()=>setPlan(null)} className="vow-btn-ghost">Back</button><button onClick={handleCreate} disabled={saving} className="vow-btn-primary flex-1">{saving?'Locking in...':'Lock in VOW'}</button></div></div></div>;
  if(clarification)return <div><button onClick={()=>{setClarification(null);setAnswers([]);}} className="text-sm text-vow-muted hover:text-vow-ink mb-6">← Adjust goal</button><PageHeader title="A few questions first" subtitle="Only answer what will make this VOW more useful."/><div className="max-w-xl space-y-6">{clarification.questions.map((question,index)=><div key={`${index}-${question}`}><label className="vow-label block mb-2">{question}</label><textarea value={answers[index]||''} onChange={e=>setAnswers(x=>x.map((a,i)=>i===index?e.target.value:a))} rows={3} className="vow-input resize-none" placeholder="Your answer..."/></div>)}<p className="text-xs text-vow-muted">{clarification.rationale}</p>{error&&<p className="text-sm text-vow-ink border-l-2 border-vow-ink pl-3">{error}</p>}<div className="flex gap-3"><button onClick={()=>{setClarification(null);setAnswers([]);}} className="vow-btn-ghost">Back</button><button onClick={buildPlan} disabled={busy} className="vow-btn-primary flex-1">{busy?'Building plan...':'Build my plan'}</button></div></div></div>;
  return <div><button onClick={onCancel} className="text-sm text-vow-muted hover:text-vow-ink mb-6">← Back</button><PageHeader title="Create a VOW" subtitle="Tell VOW what you're committing to. Armadillo will work out what it means."/><div className="max-w-xl space-y-6"><div><label className="vow-label block mb-2">Your VOW</label><textarea value={rawInput} onChange={e=>setRawInput(e.target.value)} rows={4} className="vow-input resize-none" placeholder="e.g. I want to run a 10K under an hour"/></div><div><label className="vow-label block mb-2">Why does it matter?</label><textarea value={why} onChange={e=>setWhy(e.target.value)} rows={3} className="vow-input resize-none" placeholder="Optional"/></div><div><p className="vow-label mb-3">How long?</p><div className="grid grid-cols-2 sm:grid-cols-4 gap-2">{DURATIONS.map(option=><button key={option.weeks} onClick={()=>setDurationWeeks(option.weeks)} className={`border p-3 text-left ${durationWeeks===option.weeks?'border-vow-ink':'border-vow-border'}`}><p className="text-sm text-vow-ink">{option.label}</p><p className="text-xs text-vow-muted mt-1">{option.detail}</p></button>)}</div></div><div><p className="vow-label mb-3">Which days work?</p><div className="flex flex-wrap gap-2">{DAYS.map(day=><button key={day} onClick={()=>toggleDay(day)} className={`border px-3 py-2 text-xs ${availableDays.includes(day)?'border-vow-ink text-vow-ink':'border-vow-border text-vow-muted'}`}>{day}</button>)}</div></div>{error&&<p className="text-sm text-vow-ink border-l-2 border-vow-ink pl-3">{error}</p>}<button onClick={startPlanning} disabled={busy||!rawInput.trim()||availableDays.length===0} className="vow-btn-primary w-full">{busy?'Understanding your VOW...':'Build my plan'}</button></div></div>;
}
