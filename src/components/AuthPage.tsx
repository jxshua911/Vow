import { useState } from 'react';
import { Capacitor } from '@capacitor/core';
import { Browser } from '@capacitor/browser';
import { supabase } from '@/lib/supabase';
import { checkVowAccess } from '@/lib/accessGate';
import { NATIVE_OAUTH_REDIRECT } from '@/lib/nativeAuth';
import { GoogleIcon } from './GoogleIcon';
import { BrandLogo } from './BrandLogo';

function Glyph({ children, className = '' }: { children: string; className?: string }) { return <span aria-hidden="true" className={`inline-flex items-center justify-center font-medium leading-none ${className}`}>{children}</span>; }

export function AuthPage() {
  const [emailMode, setEmailMode] = useState(false); const [mode, setMode] = useState<'signin' | 'signup'>('signin'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false);
  const openEmail = (next: 'signin' | 'signup' = 'signin') => { setMode(next); setEmailMode(true); setError(null); }; const closeEmail = () => { if (!loading) { setEmailMode(false); setError(null); setEmail(''); setPassword(''); } };
  async function assertAccess() { const access = await checkVowAccess(); if (!access.allowed) throw new Error(access.message || 'Access to VOW is restricted from this network.'); }
  async function handleSubmit(e: React.FormEvent) { e.preventDefault(); if (loading) return; const cleanEmail = email.trim(); if (!cleanEmail || !password) { setError('Please enter your email and password.'); return; } setError(null); setLoading(true); try { await assertAccess(); const result = mode === 'signup' ? await supabase.auth.signUp({ email: cleanEmail, password }) : await supabase.auth.signInWithPassword({ email: cleanEmail, password }); if (result.error) throw result.error; if (mode === 'signup' && !result.data.session) setError('Account created. Check your email to confirm your account.'); } catch (err) { setError(err instanceof Error ? err.message : 'Something went wrong. Please try again.'); } finally { setLoading(false); } }
  async function handleOAuthSignIn() { if (loading) return; setError(null); setLoading(true); let finished: { remove: () => Promise<void> } | null = null; try { await assertAccess(); const options = Capacitor.isNativePlatform() ? { redirectTo: NATIVE_OAUTH_REDIRECT, skipBrowserRedirect: true } : { redirectTo: window.location.origin }; const { data, error: oauthError } = await supabase.auth.signInWithOAuth({ provider: 'google', options }); if (oauthError) throw oauthError; if (!data?.url) throw new Error('Unable to start sign-in. Please try again.'); if (Capacitor.isNativePlatform()) { finished = await Browser.addListener('browserFinished', () => { setLoading(false); void finished?.remove(); finished = null; }); await Browser.open({ url: data.url, presentationStyle: 'popover' }); } else setLoading(false); } catch (err) { if (finished) await finished.remove(); finished = null; setError(err instanceof Error ? err.message : 'Sign-in failed. Please try again.'); setLoading(false); } }
  return <div className="min-h-screen bg-vow-bg flex flex-col items-center justify-center px-6 py-12"><div className="w-full max-w-sm"><div className="text-center mb-12"><BrandLogo className="w-44 max-w-full h-auto mx-auto mb-7" /><p className="text-vow-muted text-sm tracking-wide">Commit. Schedule. Execute. Review. Adjust.</p></div>{!emailMode ? <div className="space-y-3"><button onClick={handleOAuthSignIn} disabled={loading} className="w-full flex items-center justify-center gap-2.5 border border-vow-border py-3 text-sm font-medium text-vow-ink hover:border-vow-ink transition-colors disabled:opacity-40"><GoogleIcon className="w-4 h-4" />Continue with Google</button><button onClick={() => openEmail('signin')} disabled={loading} className="w-full flex items-center justify-center gap-2.5 bg-vow-ink text-vow-bg py-3 text-sm font-medium hover:opacity-85 transition-opacity disabled:opacity-40"><Glyph className="text-base">@</Glyph>Sign in with email</button>{error && <p className="text-sm text-vow-ink leading-relaxed pt-3 border-l-2 border-vow-ink pl-3">{error}</p>}<p className="text-xs text-vow-muted mt-8 text-center leading-relaxed">New to VOW? <button onClick={() => openEmail('signup')} className="text-vow-ink underline underline-offset-2">Create an account with email</button></p></div> : <div><button onClick={closeEmail} disabled={loading} className="text-sm text-vow-muted hover:text-vow-ink mb-6 flex items-center gap-1 transition-colors disabled:opacity-40"><Glyph>←</Glyph>Back</button><div className="flex border border-vow-border mb-8"><button onClick={() => setMode('signin')} disabled={loading} className={`flex-1 py-3 text-sm ${mode === 'signin' ? 'bg-vow-ink text-vow-bg font-medium' : 'text-vow-muted'}`}>Sign in</button><button onClick={() => setMode('signup')} disabled={loading} className={`flex-1 py-3 text-sm border-l border-vow-border ${mode === 'signup' ? 'bg-vow-ink text-vow-bg font-medium' : 'text-vow-muted'}`}>Create account</button></div><form onSubmit={handleSubmit} className="space-y-5"><div><label className="vow-label block mb-2">Email</label><div className="relative"><Glyph className="absolute left-3 top-1/2 -translate-y-1/2 text-vow-muted">@</Glyph><input type="email" required autoComplete="email" value={email} onChange={e => setEmail(e.target.value)} className="vow-input pl-10" placeholder="you@example.com" autoFocus disabled={loading} /></div></div><div><label className="vow-label block mb-2">Password</label><div className="relative"><Glyph className="absolute left-3 top-1/2 -translate-y-1/2 text-vow-muted">•</Glyph><input type="password" required minLength={6} autoComplete={mode === 'signup' ? 'new-password' : 'current-password'} value={password} onChange={e => setPassword(e.target.value)} className="vow-input pl-10" placeholder="At least 6 characters" disabled={loading} /></div></div>{error && <p className="text-sm text-vow-ink leading-relaxed border-l-2 border-vow-ink pl-3">{error}</p>}<button type="submit" disabled={loading} className="w-full flex items-center justify-center gap-2 bg-vow-ink text-vow-bg text-sm font-medium py-3 disabled:opacity-40">{loading ? 'Please wait...' : mode === 'signup' ? 'Create account' : 'Sign in'}<Glyph>→</Glyph></button></form><p className="text-xs text-vow-muted mt-8 text-center leading-relaxed">{mode === 'signup' ? 'Your journal and goals are private to you. No data is shared with third parties.' : 'Welcome back. Pick up where you left off.'}</p></div>}</div></div>;
}
